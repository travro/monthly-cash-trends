import { PageNumbersDirective } from './page-numbers.directive';

describe('PageNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new PageNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
